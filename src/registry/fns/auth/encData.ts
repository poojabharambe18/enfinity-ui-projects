// import JSEncrypt from "jsencrypt";
import React from "react";
// const encDataAPI = () => {
//   let encryptObj;
//   const inititateKey = (publicKey) => {
//     encryptObj = new JSEncrypt();
//     encryptObj.setPublicKey(publicKey);
//   };
//   const GetEncryptData = (data) => {
//     if (Boolean(data)) {
//       if (!Boolean(encryptObj)) {
//         inititateKey(process?.env?.REACT_APP_PUBLIC_KEY);
//       }
//       let encrypted = encryptObj.encrypt(data);
//       return encrypted;
//     } else {
//       return data;
//     }
//   };
//   return { inititateKey, GetEncryptData };
// };

// export const CryptoSDK = encDataAPI();
